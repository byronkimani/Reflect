import 'package:flutter/material.dart';

class BacklogPage extends StatelessWidget {
  const BacklogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Backlog Page')),
    );
  }
}
